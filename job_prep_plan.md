# 취업 준비 및 실물 로봇 테스트 실행 계획서 (Plan)

본 계획서는 오늘 남은 2.5시간 내에 실물 로봇 장비(FrontJet)를 활용해 면접관을 설득할 핵심 정량 데이터와 시각 자료를 확보하는 프로세스를 정의합니다. 사용자의 승인 이후 코드를 적용하고 문서를 업데이트합니다.

---

## [Phase 1] 사이클 타임 측정 스크립트 실행 (30분 소요 예정)
1. **스크립트 주입**: 
   * 인계 문서에 제공된 사이클 타임 측정용 파이썬 스크립트를 작업 공간의 로봇 제어 노드(예: `vision_pick_place_node.py` 또는 `verify.py`) 내 테스트 루틴에 삽입합니다.
2. **반복 테스트 10회 수행**:
   * 로봇을 안전하게 구동시키고 파이프라인을 10회 반복 실행하여 측정값을 도출합니다.
3. **결과 수집**:
   * 비전 파이프라인 평균/최대 지연시간 (ms 단위)
   * 전체 Pick & Place 평균/최대 소요시간 (초 단위)

---

## [Phase 2] 시각 포트폴리오(영상/GIF) 확보 (1시간 소요 예정)
스마트폰을 고정한 상태에서 다음 3가지 핵심 클립을 촬영합니다.
1. **Before 영상**: y축 16mm 오차를 일부러 재현하여 상자를 빗겨 잡는 모습 (트러블슈팅의 시작점 증명)
2. **After 영상**: 오프셋 보정 후 여러 위치에 상자를 두어도 정확하게 중앙을 파지(Grasp)하는 성공 영상 (8회 연속 파지의 시각화)
3. **모니터 오버레이 화면**: OBB Bounding Box가 씌워지고 좌표(cx, cy, theta)가 실시간으로 추출되는 PC 화면 녹화 (UI/디버그 뷰어)

---

## [Phase 3] Hand-Eye 재캘리브레이션 (시간 여유 시, 30분)
1. **캘리브레이션 자세 초기화**: 로봇 관절을 움직여 체커보드가 잘 보이는 25개 이상의 포즈를 재지정.
2. **TCP/Flange 확인**: `set_end_type(0)`(flange 모드) 상태로 데이터셋 추출됨을 엄격히 확인 후 캘리브레이션 연산 수행.
3. **오차 검증**: 새로운 번역벡터를 기반으로 체계적 16mm 오차가 사라졌는지 최종 확인.

---

## [Phase 4] 문서 업데이트 반영 (테스트 완료 후)
1. **포트폴리오 파일 업데이트**:
   * 대상: `/mnt/user-data/outputs/portfolio_vision_pick_place_v2.md`
   * 내용: '7. 최종 성능 결과' 탭 하단에 시스템 응답 성능 (ms/sec 수치) 추가.
2. **이력서 업데이트**:
   * 대상: `/mnt/user-data/outputs/김진호_로봇SW엔지니어_이력서.docx` 
   * 내용: 프로젝트 설명 내 정량적 속도 지표 문구 추가.

---

## 사용자 검토 및 요구사항 (피드백 요청)
> [!IMPORTANT]
> 본 계획서(`job_prep_plan.md`)를 검토해 주십시오. 
> 랩실 로봇 테스트 환경이 준비되셨다면 **"승인"** 또는 **"스크립트 적용 시작"**이라고 말씀해 주시면, 사이클 타임 측정을 위한 코드를 기존 `verify.py` 등 테스트 환경에 병합하는 작업을 시작하겠습니다. 
